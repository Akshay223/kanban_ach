# ── Kanban Sync environment variables ──
# Copy this file to ".env" and change the password before deploying.
#
#   cp .env.example .env
#
# The database password — change this to something secure!
DB_PASSWORD=kanban_secret_2024
